function add(x, y) {
    return (x + y);
}

var subtract = function (x, y) {
    return (x - y);
}

var multiply = (a, b) => {
    return (a * b);
}

var divide = (m, n) => (m / n);

var addResult = add(12, 13);
var subResult = subtract(12, 4);

console.log(addResult)
console.log(subResult)